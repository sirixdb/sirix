# Fast-query additional-try experiment

Twenty fresh JVM cohorts using unchanged aa4d81d54 compiled classes. This is a selected five-query component, never a full C6A score. The plan fixes 33 tries per selected query; reported prefixes are descriptive and do not reconstruct a global k-try protocol.

Extract raw.tar.gz into a fresh worktree-local directory, then run `python3 analyze.py --raw-root EXTRACTED --out FRESH_REPORT`. Install the rig requirements first. The replay reads logs only and does not launch Java. `raw-members.json` records the whitelist and SHA256 for every member; no database, corpus or symlink is included. Historical controller/command paths retain provenance and are not relocation-safe launch instructions.

See [the report](../../../../../../../docs/CLICKBENCH_RIG_FAST_TRIES_2026-09-08.md).
