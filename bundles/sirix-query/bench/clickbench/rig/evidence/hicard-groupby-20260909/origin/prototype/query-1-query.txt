subsequence(for $r in jn:doc('partial','records')[] let $id := $r.id, $bucket := $r.bucket group by $id, $bucket let $n := count($r) order by $n descending return {"id":$id,"bucket":$bucket,"n":$n,"s":sum($r.value),"a":avg($r.value)},1,13)