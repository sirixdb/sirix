{
  "protocol": "split-cooldown-page-cache-drop",
  "planned_composites": 20,
  "parts": [
    "0-20",
    "21-42"
  ],
  "source_head": "aa4d81d547fb0e2353ede959786d6e8ba442edf2",
  "unchanged": "captured original command/classpath, all three tries, 6/14 GiB heap, 10 GiB arena, 5 GiB eager residency, C2 flag",
  "power": "verify PL1=PL2=50000000 uW; never tune",
  "eviction": "drop_caches=1 once before cooldown for each fresh half JVM; never between query tries",
  "privilege_argv": [
    "sudo",
    "-n",
    "/usr/bin/tee",
    "/proc/sys/vm/drop_caches"
  ],
  "privilege_stdin": "1\n",
  "authorization": "PENDING Firstmate/captain mechanism; no eviction performed",
  "stopping": "all 20 composites independent of score; failures/steers stop without replacing observations",
  "sensitivity": "separate fixed paired CPU-resource perturbation study still to be finalized; lower noise alone is insufficient"
}
